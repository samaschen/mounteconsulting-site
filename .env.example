# Get a key at https://resend.com — required for server-side email from the contact form.
# Without this, the form falls back to opening a mailto: draft to ruochen.chen@mounteconsulting.com.
RESEND_API_KEY=

# Optional: verified sender, e.g. "MountE <hello@yourdomain.com>"
# RESEND_FROM=
